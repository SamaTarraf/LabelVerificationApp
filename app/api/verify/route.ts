// POST /api/verify — one image + one application JSON in, one VerificationResult out.
// Fully stateless: nothing persisted, unchanged by the batch persistence work.
// Stub: real handler not yet implemented.
export {};
