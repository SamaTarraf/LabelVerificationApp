// BatchUploadPanel — two-zone batch upload (CSV zone + image zone), template link,
// preflight pairing summary + confirm, uploads images directly to Blob, registers the
// batch, then polls /api/batch/[id]/process in a loop while its tab holds the lock.
// Stub: real implementation not yet written.
